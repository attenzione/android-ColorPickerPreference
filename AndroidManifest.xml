<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="net.margaritov"
      android:versionCode="0111"
      android:versionName="1.11">
   
 	<application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".testcolorpicker.Test"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Color picker called from Activiy-->
        <activity android:name=".activity.colorpicker.TestColorPickerActivity"
                  android:label="@string/colorpicker_standalone"/>
        
         <!-- Color picker called from PreferenceActiviy-->
		<activity android:name=".preference.colorpicker.TestColorPickerPreferenceActivity"
                  android:label="@string/colorpicker_preference"/>
	
    </application>
     
 
        
    <uses-sdk android:minSdkVersion="7" />

</manifest> 